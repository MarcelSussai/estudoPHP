<?php 
    // Criando a conexão
    // tipo do banco mysql:
    // nome do banco dbname=dbexemplologin;
    // host do banco host=localhost
    // user do banco root (ou qual seja o usuario)
    // pass do banco "" (ou qual seja a senha)
    $conn = new PDO("mysql:dbname=dbexemplologin;host=localhost", "root", "");


?>